8);
  box-shadow:
    0 16px 32px rgba(31,90,77,.24),
    inset 0 1px 1px rgba(255,255,255,.45),
    inset 0 -8px 16px rgba(18,70,60,.12),
    0 0 0 10px rgba(255,255,255,.60),
    0 0 0 11px rgba(255,255,255,.90),
    0 0 0 13px rgba(47,105,91,.07);
  -webkit-backdrop-filter:blur(18px) saturate(150%);
  backdrop-filter:blur(18px) saturate(150%);
  display:grid;place-items:center;
}
#spinBtn{
  width:100%;height:100%;border:0;border-radius:50%;
  background:transparent;color:#163b34;font-size:21px;font-weight:700;
  cursor:pointer;letter-spacing:.05em;
  transform:none !important;
}
#spinBtn:active{transform:none !important}
#spinBtn:disabled{opacity:.65;cursor:default}
.helper{text-align:center;color:var(--muted);font-size:13px;margin-top:12px}
.resultBox{
  border-radius:20px;
  padding:20px;
  background:
    linear-gradient(145deg,rgba(218,237,231,.72),rgba(245,250,248,.56));
  border:1px solid rgba(255,255,255,.76);
  min-height:142px;
  display:flex;
  flex-direction:column;
  justify-content:center;
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,.84),
    0 10px 28px rgba(44,77,66,.06);
  -webkit-backdrop-filter:blur(18px) saturate(145%);
  backdrop-filter:blur(18px) saturate(145%);
}
.kicker{color:var(--muted);font-size:13px;margin-bottom:8px}
#result{font-size:27px;font-weight:750;letter-spacing:-.02em;word-break:break-word}
#remain{margin-top:9px;color:var(--muted);font-size:14px}
.controls{display:flex;gap:9px;flex-wrap:wrap;margin-top:14px}
button,.btn{
  border:1px solid rgba(98,117,108,.18);
  background:rgba(255,255,255,.60);
  color:var(--text);
  border-radius:13px;
  padding:10px 14px;
  cursor:pointer;
  transition:.16s ease;
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,.72),
    0 2px 8px rgba(45,68,60,.03);
  -webkit-backdrop-filter:blur(12px) saturate(140%);
  backdrop-filter:blur(12px) saturate(140%);
}
button:hover,.btn:hover{border-color:#cdd6d1;background:#fbfcfb}
button:active:not(#spinBtn){transform:translateY(1px)}
button.primary{background:var(--accent);border-color:var(--accent);color:white}
button.danger{color:var(--danger)}
.switchLine{
  display:flex;align-items:center;gap:9px;color:var(--muted);font-size:14px
}
.switchLine input{width:18px;height:18px}
.sectionTitle{font-size:16px;font-weight:700;margin:22px 0 10px}
.history{display:grid;gap:8px}
.hist{
  display:flex;justify-content:space-between;gap:10px;
  padding:10px 12px;border:1px solid var(--line);border-radius:12px;
  background:var(--panel2);font-size:14px
}
.note{font-size:13px;color:var(--muted);line-height:1.65}
.editorPanel{margin-top:18px;padding:20px}
.toolbar{display:flex;gap:8px;flex-wrap:wrap;margin:11px 0 8px}
.itemRow{
  display:grid;grid-template-columns:42px 1fr auto;gap:10px;align-items:center;
  padding:9px 0;border-bottom:1px solid var(--line)
}
.itemRow:last-child{border-bottom:none}
.num{
  width:38px;height:38px;border-radius:12px;background:var(--accent-softer);
  color:var(--accent-deep);display:grid;place-items:center;font-weight:700
}
.itemRow input{
  width:100%;border:1px solid var(--line);border-radius:12px;
  padding:10px 12px;background:var(--panel2);color:var(--text);outline:none
}
.itemRow input:focus{border-color:var(--accent);box-shadow:0 0 0 3px rgba(46,117,103,.10)}
.fileInput{display:none}
.footer{text-align:center;color:var(--muted);font-size:12px;padding:18px 0 2px}
.badge{
  display:inline-flex;align-items:center;gap:6px;border-radius:999px;
  background:var(--accent-softer);color:var(--accent-deep);
  padding:6px 9px;font-size:12px;font-weight:600
}
.dot{width:7px;height:7px;border-radius:50%;background:var(--accent)}
@media(max-width:860px){
  .wrap{padding:14px}
  header{align-items:flex-start;flex-direction:column}
  .layout{grid-template-columns:1fr}
  .panel{border-radius:20px}
  .wheelPanel,.sidePanel,.editorPanel{padding:14px}
  h1{font-size:23px}
  .centerDisc{width:96px;height:96px}
  #spinBtn{font-size:18px}
}
@media(prefers-color-scheme:dark){
  :root{
    --bg:#151816;
    --panel:rgba(31,36,33,.66);
    --panel-solid:#202421;
    --panel2:rgba(36,41,37,.58);
    --text:#f1f4f2;
    --muted:#a4aca7;
    --line:rgba(255,255,255,.10);
    --accent:#6cb2a2;
    --accent-deep:#4c9384;
    --accent-soft:rgba(54,89,79,.52);
    --accent-softer:rgba(37,57,51,.56);
    --wheel-a:rgba(60,95,85,.48);
    --wheel-b:rgba(36,42,38,.58);
    --glass-edge:rgba(255,255,255,.11);
    --glass-shadow:rgba(0,0,0,.28);
    --shadow:none;
  }
  body{
    background:
      radial-gradient(circle at 14% 10%,rgba(83,127,114,.12),
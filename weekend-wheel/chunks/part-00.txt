<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<title>周末去哪儿 · 质感随机转盘 · 可编辑版</title>
<style>
:root{
  --bg:#eef2ef;
  --panel:rgba(255,255,255,.66);
  --panel-solid:#ffffff;
  --panel2:rgba(248,250,249,.58);
  --text:#202522;
  --muted:#727a75;
  --line:rgba(89,108,99,.16);
  --accent:#317b6c;
  --accent-deep:#255f54;
  --accent-soft:rgba(188,220,211,.46);
  --accent-softer:rgba(224,239,234,.52);
  --wheel-a:rgba(198,225,218,.52);
  --wheel-b:rgba(255,255,255,.50);
  --glass-edge:rgba(255,255,255,.78);
  --glass-shadow:rgba(36,62,53,.13);
  --danger:#a65353;
  --shadow:0 18px 50px rgba(27,44,38,.08)
}
*{box-sizing:border-box}
html{-webkit-text-size-adjust:100%}
body{
  margin:0;
  font-family:-apple-system,BlinkMacSystemFont,"Segoe UI","PingFang SC",
  "Hiragino Sans GB","Microsoft YaHei",sans-serif;
  color:var(--text);
  background:
    radial-gradient(circle at 15% 10%, rgba(255,255,255,.92), transparent 30%),
    radial-gradient(circle at 85% 18%, rgba(170,211,200,.18), transparent 26%),
    linear-gradient(145deg,#f3f6f4 0%,#e9efec 100%);
  background-attachment:fixed;
}
button,input{font:inherit}
button{-webkit-tap-highlight-color:transparent}
.wrap{max-width:1120px;margin:0 auto;padding:22px}
header{
  display:flex;align-items:flex-end;justify-content:space-between;
  gap:18px;margin-bottom:18px
}
h1{margin:0;font-size:27px;letter-spacing:-.02em}
.sub{margin-top:7px;color:var(--muted);font-size:14px}
.layout{display:grid;grid-template-columns:minmax(0,1.08fr) minmax(320px,.92fr);gap:18px}
.panel{
  background:rgba(255,255,255,.62);
  border:1px solid rgba(255,255,255,.72);
  border-radius:26px;
  box-shadow:
    0 18px 50px rgba(31,52,45,.08),
    inset 0 1px 0 rgba(255,255,255,.82),
    inset 0 -1px 0 rgba(70,96,86,.05);
  -webkit-backdrop-filter:blur(24px) saturate(145%);
  backdrop-filter:blur(24px) saturate(145%);
}
.wheelPanel{padding:20px}
.sidePanel{padding:20px}
.wheelStage{
  width:min(100%,620px);aspect-ratio:1/1;margin:auto;position:relative;
  display:grid;place-items:center
}
.wheelShell{
  position:absolute;inset:1.5%;
  border-radius:50%;
  background:
    radial-gradient(circle at 34% 25%, rgba(255,255,255,.96), rgba(255,255,255,.42) 34%, rgba(223,233,228,.34) 72%, rgba(196,211,204,.24));
  border:1px solid rgba(255,255,255,.88);
  -webkit-backdrop-filter:blur(22px) saturate(150%);
  backdrop-filter:blur(22px) saturate(150%);
  box-shadow:
    0 26px 60px rgba(30,57,48,.14),
    0 7px 22px rgba(30,57,48,.07),
    inset 0 2px 0 rgba(255,255,255,.92),
    inset 0 -8px 18px rgba(70,100,89,.07),
    inset 0 0 0 1px rgba(255,255,255,.48);
}
#wheelRotor{
  position:absolute;inset:4.5%;
  transform:rotate(0deg);
  transform-origin:50% 50%;
  will-change:transform;
  border-radius:50%;
  overflow:hidden;
  filter:
    drop-shadow(0 12px 18px rgba(29,55,46,.08))
    drop-shadow(0 2px 3px rgba(255,255,255,.7));
}
#wheelSvg{width:100%;height:100%;display:block;overflow:visible}
.rim{
  position:absolute;inset:3.5%;border-radius:50%;
  pointer-events:none;
  border:1px solid rgba(255,255,255,.76);
  box-shadow:
    inset 0 0 0 2px rgba(44,87,73,.08),
    inset 0 0 0 9px rgba(255,255,255,.22),
    inset 0 18px 28px rgba(255,255,255,.18),
    0 1px 0 rgba(255,255,255,.88);
}

.pointerAssembly{
  position:absolute;
  right:-2px;
  top:50%;
  width:108px;
  height:58px;
  transform:translateY(-50%) rotate(0deg);
  transform-origin:100% 50%;
  z-index:10;
  pointer-events:none;
}
.pointerAssembly.bump{
  animation:assemblyBump .13s ease-out;
}
@keyframes assemblyBump{
  0%{transform:translateY(-50%) rotate(0deg)}
  45%{transform:translateY(-50%) rotate(7deg)}
  100%{transform:translateY(-50%) rotate(0deg)}
}
.pointerWrap{
  position:absolute;
  right:0;
  top:50%;
  transform:translateY(-50%);
  z-index:2;
  width:62px;
  height:58px;
  display:grid;
  place-items:center end;
  pointer-events:none;
}
.pointer{
  width:0;height:0;
  border-top:15px solid transparent;
  border-bottom:15px solid transparent;
  border-right:34px solid var(--accent-deep);
  filter:drop-shadow(-4px 2px 4px rgba(0,0,0,.18));
  transform-origin:100% 50%;
}
  45%{transform:rotate(7deg)}
  100%{transform:rotate(0)}
}
.centerDisc{
  position:absolute;left:50%;top:50%;transform:translate(-50%,-50%);
  width:116px;height:116px;border-radius:50%;z-index:12;
  background:
    linear-gradient(155deg,rgba(65,137,121,.96),rgba(34,96,83,.98));
  border:1px solid rgba(255,255,255,.4